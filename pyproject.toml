[project]
name = "teaching-repository-template"
version = "0.1.0"
description = "Reusable Quarto teaching repository template"
requires-python = ">=3.11"
dependencies = [
  "jupyter>=1.1.1",
  "PyYAML>=6.0.2",
]
